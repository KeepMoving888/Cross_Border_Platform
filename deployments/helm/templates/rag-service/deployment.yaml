{{- if .Values.ragService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "cb-platform.fullname" . }}-rag
  labels:
    {{- include "cb-platform.componentLabels" (dict "ctx" . "component" "rag") | nindent 4 }}
spec:
  {{- if not .Values.ragService.hpa.enabled }}
  replicas: {{ .Values.ragService.replicas }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "cb-platform.selectorLabels" (dict "ctx" . "component" "rag") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "cb-platform.selectorLabels" (dict "ctx" . "component" "rag") | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/rag-service/configmap.yaml") . | sha256sum }}
    spec:
      {{- with .Values.image.pullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: cb-rag
          image: {{ include "cb-platform.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.ragService.port }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "cb-platform.fullname" . }}-rag-config
          env:
            - name: APP_ROLE
              value: {{ .Values.ragService.role | quote }}
            - name: APP_PORT
              value: {{ .Values.ragService.port | quote }}
            {{- if .Values.otel.enabled }}
            - name: OTEL_ENDPOINT
              value: {{ .Values.otel.endpoint | quote }}
            - name: OTEL_SAMPLING_RATIO
              value: {{ .Values.otel.samplingRatio | quote }}
            {{- end }}
          resources:
            {{- toYaml .Values.ragService.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 10"]
      terminationGracePeriodSeconds: 60
      {{- with .Values.ragService.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.ragService.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.ragService.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
